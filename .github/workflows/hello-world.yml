name: hello-world

on:
  push:
    branches:
      - 'main'
      
  workflow_dispatch:
    inputs:
      input:
        description: "Název API, které se má publikovat"
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Test2
        shell: bash
        run: |
          echo "::notice title=API url::$(grep location headers | awk '{print $2}')"
