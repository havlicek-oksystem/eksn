name: PR verification GitLeaks

on:
  pull_request:
    types: 'opened'
    branches:
      - 'main'

jobs:
  gitleaks_scan:
    runs-on: ubuntu-latest

    steps:
      - name: Gitleaks Scan
        shell: bash
        run: |
          echo "Gitleaks result:" >> output
          echo "Nalezeleno nula secrets leaks." >> output
          echo "Tento komantar byl vytvoren automaticky v ramci Gitleaks scanu na PR." >> output

      - name: Comment on PR
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.KLICEK }}
          script: |
            fetch("output");
            console.log(title);
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: 'Tento komentar byl vytvoren automaticky v ramci testu komentovani na PR.'
            })